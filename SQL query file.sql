create database bfsi;
use bfsi;

select * from company limit 2;

SELECT COUNT(*) FROM company;

DESCRIBE company;

CREATE TABLE cleaned_loan_data  
AS SELECT 
SK_ID_CURR,TARGET,NAME_CONTRACT_TYPE,CNT_CHILDREN,
AMT_INCOME_TOTAL,AMT_CREDIT,AMT_ANNUITY,NAME_INCOME_TYPE,
NAME_EDUCATION_TYPE,NAME_FAMILY_STATUS,NAME_HOUSING_TYPE,
REGION_RATING_CLIENT,REGION_POPULATION_RELATIVE,
EXT_SOURCE_1,EXT_SOURCE_2,EXT_SOURCE_3,

### CODE_GENDER cleaning
CASE WHEN CODE_GENDER = 'XNA' 
THEN 'Unknown' ELSE 
CODE_GENDER 
END AS 
CODE_GENDER,

-- AGE_YEARS (DAYS_BIRTH se) 
FLOOR(-DAYS_BIRTH / 365) AS AGE_YEARS,

-- 3. YEARS_EMPLOYED (from DAYS_EMPLOYED ) 
CASE WHEN DAYS_EMPLOYED = 365243 
THEN NULL ELSE FLOOR(-DAYS_EMPLOYED / 365)
 END AS 
 YEARS_EMPLOYED,
    
-- 4. OCCUPATION_TYPE cleaning
CASE WHEN 
OCCUPATION_TYPE IS NULL THEN 'Unknown' 
ELSE OCCUPATION_TYPE END AS 
OCCUPATION_TYPE,
   
-- 5. INCOME_TO_CREDIT_RATIO (naya derived column)
  ROUND(AMT_INCOME_TOTAL / AMT_CREDIT, 4)
  AS INCOME_TO_CREDIT_RATIO
  FROM company;
    
select * from cleaned_loan_data;

    